name: Python application

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

permissions:
  contents: read

jobs:
  commit-check:
    uses: Sz-Ltc/.github/.github/workflows/reusable-commit-check.yml@main
    with:
      python-version: '3.11'
    secrets:
      ORG_CI_TOKEN: ${{ secrets.ORG_CI_TOKEN }}
